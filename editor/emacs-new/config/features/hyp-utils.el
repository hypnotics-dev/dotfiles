;; Various helper functions for configuration

;; checks the dependencies found in hyp/emacs-deps, by first flattening and the filtering for uniq elements.
;; (defun hyp/deps 
;;   (let (deps (-uniq (-flatten hyp/emacs-dep)))
;;        ))
